/* INVICTO OPS v32 · Rendimiento estable, independiente y no bloqueante */
const PERF32_CUTOVER_DATE='2026-09-15';
let perf32Data=null;
let perf32Date='';
let perf32Loading=false;
let perf32Advisor='';
let perf32Status='';
let perf32Search='';

function perf32Today(){return new Intl.DateTimeFormat('en-CA',{timeZone:'America/Bogota',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())}
function perf32DefaultDate(){const t=perf32Today();return t<PERF32_CUTOVER_DATE?PERF32_CUTOVER_DATE:t}
function perf32Esc(v=''){return typeof esc==='function'?esc(v):String(v)}
function perf32Money(v){return typeof money==='function'?money(v):new Intl.NumberFormat('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0}).format(Number(v||0))}
function perf32DateTime(v){if(!v)return '—';const d=new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleString('es-CO',{timeZone:'America/Bogota',day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}
function perf32Minutes(v){if(v==null)return '—';v=Number(v);if(!Number.isFinite(v))return '—';if(v<60)return `${v.toFixed(v<10?1:0)} min`;if(v<1440)return `${(v/60).toFixed(1)} h`;return `${(v/1440).toFixed(1)} d`}
function perf32Pct(v){return v==null?'—':`${Number(v||0).toFixed(1)}%`}
function perf32StatusLabel(st=''){return ({nueva:'Nueva',asignada:'Asignada',en_gestion:'En gestión',no_contesta:'No contesta',seguimiento_programado:'Seguimiento programado',pendiente_stock:'Pendiente stock',confirmada:'Confirmada',perdida:'Perdida',duplicada:'Duplicada · no contabiliza',cancelada:'Cancelada'})[st]||st||'—'}
function perf32Task(r={}){if(r.followup_overdue)return 'ATENDER AHORA · seguimiento vencido';return ({nueva:'Contactar cliente',asignada:'Primer contacto',en_gestion:'Continuar gestión',no_contesta:'Reintentar contacto',seguimiento_programado:'Cumplir seguimiento',pendiente_stock:'Validar stock',confirmada:'Terminada · confirmada',perdida:'Terminada · perdida',duplicada:'No gestionar · duplicada',cancelada:'Terminada · cancelada'})[r.status]||'Revisar venta'}
function perf32Next(r={}){if(r.followup_overdue)return 'VENCIDO';if(r.next_followup_at)return perf32DateTime(r.next_followup_at);if(['confirmada','perdida','duplicada','cancelada'].includes(r.status))return 'Finalizada';return perf32Task(r)}
function perf32StatusClass(st=''){if(st==='confirmada')return 'green';if(['perdida','cancelada'].includes(st))return 'red';if(st==='duplicada')return 'gray';if(['no_contesta','seguimiento_programado','pendiente_stock'].includes(st))return 'amber';return 'blue'}

function perf32Styles(){if(document.getElementById('perf32Styles'))return;const s=document.createElement('style');s.id='perf32Styles';s.textContent=`
.perf32-head{display:flex;gap:16px;justify-content:space-between;align-items:flex-start;margin-bottom:14px}.perf32-head h1{margin:3px 0 5px}.perf32-actions{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.perf32-banner{padding:13px 15px;border:1px solid #cce9da;background:#effaf4;color:#195f3e;border-radius:14px;margin-bottom:14px}.perf32-banner.wait{background:#fff8e9;border-color:#ffe0a2;color:#7a5200}.perf32-cards{display:grid;grid-template-columns:repeat(6,minmax(130px,1fr));gap:10px;margin-bottom:14px}.perf32-card{background:#fff;border:1px solid #e5e9ef;border-radius:16px;padding:14px}.perf32-card small{display:block;color:#6f7a90}.perf32-card b{display:block;font-size:25px;color:#09132a;margin:4px 0}.perf32-card span{font-size:12px;color:#7b8597}.perf32-table{min-width:1900px}.perf32-table th,.perf32-table td{white-space:nowrap}.perf32-good{color:#118159;font-weight:900}.perf32-bad{color:#bd3e2c;font-weight:900}.perf32-toolbar{display:flex;gap:8px;flex-wrap:wrap}.perf32-toolbar .search{min-width:180px}.perf32-section{margin-top:14px}.perf32-modal-back{position:fixed;inset:0;background:rgba(2,16,68,.48);z-index:99970}.perf32-modal{position:fixed;right:0;top:0;bottom:0;width:min(650px,94vw);background:#fff;z-index:99971;box-shadow:-16px 0 50px rgba(2,16,68,.22);overflow:auto;padding:24px}.perf32-modal-head{display:flex;gap:12px;align-items:flex-start;border-bottom:1px solid #e8ebf0;padding-bottom:16px}.perf32-modal-head button{margin-left:auto}.perf32-detail{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-top:16px}.perf32-detail>div{border:1px solid #e6eaf0;border-radius:13px;padding:12px}.perf32-detail small{display:block;color:#6d7890}.perf32-detail b{font-size:17px}.perf32-timeline{margin-top:18px}.perf32-event{position:relative;padding:0 0 17px 24px;border-left:2px solid #e5e9f0;margin-left:7px}.perf32-event:before{content:'';position:absolute;left:-7px;top:2px;width:12px;height:12px;border-radius:50%;background:#091c5a}.perf32-event small,.perf32-event p{color:#6d7890}.perf32-event b{display:block;margin:3px 0}.perf32-loading{opacity:.7}.perf32-activity{max-height:480px;overflow:auto}.perf32-act{display:grid;grid-template-columns:135px 160px 170px 1fr;gap:10px;padding:11px 14px;border-bottom:1px solid #edf0f4}.perf32-act:last-child{border-bottom:0}
@media(max-width:1200px){.perf32-cards{grid-template-columns:repeat(3,1fr)}}@media(max-width:700px){.perf32-head{display:block}.perf32-actions{margin-top:10px}.perf32-cards{grid-template-columns:repeat(2,1fr)}.perf32-detail{grid-template-columns:repeat(2,1fr)}.perf32-act{grid-template-columns:1fr}}
`;document.head.appendChild(s)}

function perf32ZeroData(date){return {date,summary:{assigned:0,workload:0,carryover:0,managed:0,untouched:0,attempts:0,pending:0,overdue:0,confirmed:0,confirmed_units:0,lost:0,cancelled:0,duplicates:0,manual_sales:0,yellow:0,red:0},advisors:[],sales:[],activity:[],can_view_all:!!isAdmin()}}
function perf32Card(label,value,sub=''){return `<div class="perf32-card"><small>${perf32Esc(label)}</small><b>${value}</b><span>${perf32Esc(sub)}</span></div>`}

function perf32AdvisorRows(){const rows=perf32Data?.advisors||[];return rows.map(r=>`<tr><td><b>${perf32Esc(r.advisor)}</b></td><td>${Number(r.assigned||0)}</td><td>${Number(r.carryover||0)}</td><td><b>${Number(r.workload||0)}</b></td><td>${Number(r.managed||0)}</td><td class="${Number(r.untouched||0)?'perf32-bad':''}">${Number(r.untouched||0)}</td><td>${Number(r.pending||0)}</td><td><b>${Number(r.attempts||0)}</b></td><td>${Number(r.avg_attempts_per_managed||0).toFixed(2)}</td><td>${Number(r.followups||0)}</td><td class="${Number(r.overdue||0)?'perf32-bad':''}">${Number(r.overdue||0)}</td><td class="perf32-good">${Number(r.confirmed||0)}</td><td>${Number(r.lost||0)}</td><td>${Number(r.cancelled||0)}</td><td>${Number(r.duplicates||0)}</td><td><b>${perf32Pct(r.conversion)}</b></td><td>${perf32Pct(r.management_rate)}</td><td>${perf32Minutes(r.avg_first_response_minutes)}</td><td>${perf32Minutes(r.max_first_response_minutes)}</td><td>${perf32Pct(r.first_response_5m_pct)}</td><td>${perf32Pct(r.first_response_10m_pct)}</td><td>${Number(r.manual_sales||0)}</td><td>${Number(r.confirmed_units||0)}</td><td><b>${perf32Money(r.confirmed_value||0)}</b></td><td>${Number(r.yellow||0)}</td><td class="${Number(r.red||0)?'perf32-bad':''}">${Number(r.red||0)}</td><td>${perf32DateTime(r.last_activity_at)}</td><td><button class="btn light sm" onclick="openPerf32Advisor('${r.id}')">Detalle</button></td></tr>`).join('')||'<tr><td colspan="28" class="muted">Sin datos todavía.</td></tr>'}

function perf32FilteredSales(){const rows=perf32Data?.sales||[],q=String(perf32Search||'').trim().toLowerCase();return rows.filter(r=>(!perf32Advisor||String(r.advisor_id)===String(perf32Advisor))&&(!perf32Status||r.status===perf32Status)&&(!q||[r.order,r.customer_name,r.customer_phone,r.customer_city,r.advisor,perf32Task(r)].some(x=>String(x||'').toLowerCase().includes(q))))}

function perf32SalesTable(){const rows=perf32FilteredSales(),statuses=[...new Set((perf32Data?.sales||[]).map(x=>x.status).filter(Boolean))],advisors=perf32Data?.advisors||[];return `<div class="panel perf32-section"><div class="panel-head"><div><h3>Venta por venta · tarea y gestión</h3><div class="muted">Quién la tiene, qué ha hecho, cuánto demoró y qué sigue.</div></div><div class="spacer"></div><span class="tag blue">${rows.length} visibles</span></div><div class="panel-head"><div class="perf32-toolbar"><input class="search" placeholder="Pedido, cliente, teléfono, ciudad o tarea…" value="${perf32Esc(perf32Search)}" oninput="setPerf32Filter('search',this.value)">${perf32Data?.can_view_all?`<select class="search" onchange="setPerf32Filter('advisor',this.value)"><option value="">Todos los asesores</option>${advisors.map(a=>`<option value="${a.id}" ${String(a.id)===String(perf32Advisor)?'selected':''}>${perf32Esc(a.advisor)}</option>`).join('')}</select>`:''}<select class="search" onchange="setPerf32Filter('status',this.value)"><option value="">Todos los estados</option>${statuses.map(st=>`<option value="${st}" ${st===perf32Status?'selected':''}>${perf32Esc(perf32StatusLabel(st))}</option>`).join('')}</select></div></div><div class="table-wrap"><table><thead><tr><th>Pedido</th><th>Cliente</th><th>Asesor</th><th>Recibida</th><th>Tarea actual</th><th>Estado</th><th>Intentos</th><th>1ª respuesta</th><th>Última gestión</th><th>Próxima acción</th><th>Sin gestión</th><th>Manual</th><th>Unidades</th><th>Valor</th><th></th></tr></thead><tbody>${rows.map(r=>`<tr><td><b>${perf32Esc(r.order||'—')}</b></td><td><b>${perf32Esc(r.customer_name||'Sin nombre')}</b><div class="muted">${perf32Esc(r.customer_phone||'')}</div></td><td>${perf32Esc(r.advisor||'Sin asignar')}</td><td>${perf32DateTime(r.received_at)}</td><td><b>${perf32Esc(perf32Task(r))}</b></td><td><span class="tag ${perf32StatusClass(r.status)}">${perf32Esc(perf32StatusLabel(r.status))}</span></td><td>${Number(r.attempts||0)}</td><td>${perf32Minutes(r.first_response_minutes)}</td><td>${perf32DateTime(r.last_managed_at)}</td><td>${perf32Esc(perf32Next(r))}</td><td>${perf32Minutes(r.minutes_without_management)}</td><td>${r.manual?'Sí':'No'}</td><td>${Number(r.quantity||0)}</td><td><b>${perf32Money(r.total_price||0)}</b></td><td><button class="btn light sm" onclick="openPerf32Sale('${r.id}')">Historial</button></td></tr>`).join('')||'<tr><td colspan="15" class="muted">Todavía no hay ventas de la nueva operación.</td></tr>'}</tbody></table></div></div>`}

function perf32Activity(){const rows=perf32Data?.activity||[];return `<div class="panel perf32-section"><div class="panel-head"><div><h3>Bitácora del día</h3><div class="muted">Asignaciones e intentos realizados en orden cronológico.</div></div><div class="spacer"></div><span class="tag gray">${rows.length} eventos</span></div><div class="perf32-activity">${rows.map(e=>`<div class="perf32-act"><b>${perf32DateTime(e.at)}</b><span>${perf32Esc(e.advisor||'Sistema')}</span><span><b>${perf32Esc(e.order||'—')}</b></span><span>${e.kind==='assigned'?'Venta recibida y asignada':`Intento #${e.attempt||''} · ${perf32Esc(e.outcome||'Gestión')}${e.channel?` · ${perf32Esc(e.channel)}`:''}${e.notes?` · ${perf32Esc(e.notes)}`:''}`}</span></div>`).join('')||'<div class="panel-body muted">Sin actividad todavía.</div>'}</div></div>`}

function paintPerformanceV32(){perf32Styles();const host=el('view-performance');if(!host)return;const d=perf32Data||perf32ZeroData(perf32Date||perf32DefaultDate()),s=d.summary||{},pre=String(d.date||'')<PERF32_CUTOVER_DATE,waiting=perf32Today()<PERF32_CUTOVER_DATE;host.innerHTML=`<div class="perf32-head"><div><div class="eyebrow">MEDICIÓN OPERATIVA UNIFICADA · DESDE 15 SEP 2026</div><h1>Rendimiento</h1><p>${d.can_view_all?'Vista total del equipo, usuario por usuario y venta por venta.':'Tu rendimiento, tus tareas y toda tu gestión en una sola pantalla.'}</p></div><div class="perf32-actions"><input type="date" class="search" value="${perf32Esc(d.date||perf32Date)}" onchange="changePerformanceV32(this.value)"><button class="btn light sm" onclick="refreshPerformanceV32()">↻ Actualizar</button></div></div>${waiting?'<div class="perf32-banner wait"><b>Preparado para iniciar.</b> La medición comienza el 15 de septiembre a las 00:00 Colombia. El tablero ya está listo y parte de 0.</div>':pre?'<div class="perf32-banner wait"><b>Sistema anterior.</b> Esta fecha permanece fuera de la medición nueva.</div>':'<div class="perf32-banner"><b>Sistema nuevo activo.</b> Todo lo que ves se calcula desde el corte operativo del 15 de septiembre.</div>'}<div class="perf32-cards ${perf32Loading?'perf32-loading':''}">${perf32Card('Asignadas hoy',s.assigned||0,'ventas nuevas')}${perf32Card('Carga total',s.workload||0,`${s.carryover||0} arrastre`)}${perf32Card('Gestionadas',s.managed||0,`${s.untouched||0} sin tocar`)}${perf32Card('Intentos',s.attempts||0,'acciones registradas')}${perf32Card('Pendientes',s.pending||0,`${s.overdue||0} vencidas`)}${perf32Card('Confirmadas',s.confirmed||0,`${s.confirmed_units||0} unidades`)}${perf32Card('Perdidas',s.lost||0,'ventas válidas')}${perf32Card('Canceladas',s.cancelled||0,'cerradas')}${perf32Card('Duplicadas',s.duplicates||0,'no contabilizan')}${perf32Card('Ventas manuales',s.manual_sales||0,'acreditadas al creador')}${perf32Card('Alertas amarillas',s.yellow||0,'nuevo sistema')}${perf32Card('Alertas rojas',s.red||0,'nuevo sistema')}</div><div class="panel perf32-section"><div class="panel-head"><div><h3>Medición completa por asesor</h3><div class="muted">Todos los indicadores operativos en una sola tabla.</div></div>${perf32Loading?'<span class="tag amber">ACTUALIZANDO…</span>':''}</div><div class="table-wrap"><table class="perf32-table"><thead><tr><th>Asesor</th><th>Asignadas</th><th>Arrastre</th><th>Carga total</th><th>Gestionadas</th><th>Sin tocar</th><th>Pendientes</th><th>Intentos</th><th>Intentos/gestión</th><th>Seguimientos</th><th>Vencidos</th><th>Confirmadas</th><th>Perdidas</th><th>Canceladas</th><th>Duplicadas</th><th>Conversión</th><th>% gestión</th><th>1ª respuesta prom.</th><th>Máx. 1ª resp.</th><th>≤5 min</th><th>≤10 min</th><th>Manuales</th><th>Unidades conf.</th><th>Valor conf.</th><th>Amarillas</th><th>Rojas</th><th>Última actividad</th><th></th></tr></thead><tbody>${perf32AdvisorRows()}</tbody></table></div></div>${perf32SalesTable()}${perf32Activity()}`}

async function loadPerformanceV32(force=false){if(perf32Loading)return;if(!perf32Date)perf32Date=perf32DefaultDate();if(!force&&perf32Data?.date===perf32Date){paintPerformanceV32();return}perf32Loading=true;if(!perf32Data)perf32Data=perf32ZeroData(perf32Date);paintPerformanceV32();try{const req=invictoSupabaseV12.rpc('get_unified_advisor_performance_v31',{p_date:perf32Date});const timeout=new Promise((_,reject)=>setTimeout(()=>reject(new Error('La consulta tardó demasiado.')),8000));const p=await Promise.race([req,timeout]);if(p.error)throw p.error;perf32Data=p.data||perf32ZeroData(perf32Date)}catch(e){console.error('performance v32',e);const host=el('view-performance');if(host)host.insertAdjacentHTML('afterbegin',`<div class="blocking"><b>No se pudo actualizar Rendimiento.</b><br>${perf32Esc(e?.message||e)} <button class="btn light sm" onclick="refreshPerformanceV32()">Reintentar</button></div>`)}finally{perf32Loading=false;paintPerformanceV32()}}

window.refreshPerformanceV32=function(){perf32Data=null;loadPerformanceV32(true)};
window.changePerformanceV32=function(v){if(!/^\d{4}-\d{2}-\d{2}$/.test(v))return;perf32Date=v;perf32Data=null;loadPerformanceV32(true)};
window.setPerf32Filter=function(k,v){if(k==='advisor')perf32Advisor=v;if(k==='status')perf32Status=v;if(k==='search')perf32Search=v;paintPerformanceV32()};

window.openPerf32Advisor=function(id){const r=(perf32Data?.advisors||[]).find(x=>String(x.id)===String(id));if(!r)return;document.getElementById('perf32ModalWrap')?.remove();const st=r.status_breakdown||{},w=document.createElement('div');w.id='perf32ModalWrap';w.innerHTML=`<div class="perf32-modal-back" onclick="closePerf32Modal()"></div><aside class="perf32-modal"><div class="perf32-modal-head"><div><div class="eyebrow">DETALLE DE ASESOR</div><h2>${perf32Esc(r.advisor)}</h2><div class="muted">${perf32Esc(perf32Data?.date||'')}</div></div><button class="btn light sm" onclick="closePerf32Modal()">×</button></div><div class="perf32-detail">${[['Asignadas',r.assigned],['Arrastre',r.carryover],['Carga total',r.workload],['Gestionadas',r.managed],['Sin tocar',r.untouched],['Pendientes',r.pending],['Intentos',r.attempts],['Seguimientos',r.followups],['Vencidos',r.overdue],['Confirmadas',r.confirmed],['Perdidas',r.lost],['Canceladas',r.cancelled],['Duplicadas',r.duplicates],['Conversión',perf32Pct(r.conversion)],['% gestión',perf32Pct(r.management_rate)],['1ª respuesta promedio',perf32Minutes(r.avg_first_response_minutes)],['1ª respuesta máxima',perf32Minutes(r.max_first_response_minutes)],['≤5 min',perf32Pct(r.first_response_5m_pct)],['≤10 min',perf32Pct(r.first_response_10m_pct)],['Manuales',r.manual_sales],['Unidades confirmadas',r.confirmed_units],['Valor confirmado',perf32Money(r.confirmed_value)],['Amarillas',r.yellow],['Rojas',r.red]].map(x=>`<div><small>${perf32Esc(x[0])}</small><b>${x[1]??0}</b></div>`).join('')}</div><div class="panel" style="margin-top:15px"><div class="panel-head"><h3>Tareas abiertas</h3></div><div class="panel-body">${Object.entries(st).map(([k,v])=>`<div class="statline"><span>${perf32Esc(perf32StatusLabel(k))}</span><b>${v}</b></div>`).join('')||'<div class="muted">Sin tareas abiertas.</div>'}</div></div></aside>`;document.body.appendChild(w)};
window.closePerf32Modal=function(){document.getElementById('perf32ModalWrap')?.remove()};

window.openPerf32Sale=async function(id){const base=(perf32Data?.sales||[]).find(x=>String(x.id)===String(id));if(!base)return;document.getElementById('perf32ModalWrap')?.remove();const w=document.createElement('div');w.id='perf32ModalWrap';w.innerHTML=`<div class="perf32-modal-back" onclick="closePerf32Modal()"></div><aside class="perf32-modal"><div class="perf32-modal-head"><div><div class="eyebrow">BITÁCORA COMPLETA</div><h2>${perf32Esc(base.order||'—')}</h2><div class="muted">${perf32Esc(base.customer_name||'')} · ${perf32Esc(base.advisor||'')}</div></div><button class="btn light sm" onclick="closePerf32Modal()">×</button></div><div class="panel-body">Cargando historial…</div></aside>`;document.body.appendChild(w);try{const {data,error}=await invictoSupabaseV12.rpc('get_ops_followup_board',{p_date:perf32Data?.date||perf32Date});if(error)throw error;const f=(data?.sales||[]).find(x=>String(x.id)===String(id)),events=f?.history||base.attempt_events_today||[];const aside=w.querySelector('.perf32-modal');aside.innerHTML=`<div class="perf32-modal-head"><div><div class="eyebrow">BITÁCORA COMPLETA</div><h2>${perf32Esc(base.order||'—')}</h2><div class="muted">${perf32Esc(base.customer_name||'')} · ${perf32Esc(base.advisor||'')}</div></div><button class="btn light sm" onclick="closePerf32Modal()">×</button></div><div class="perf32-detail"><div><small>Estado</small><b>${perf32Esc(perf32StatusLabel(base.status))}</b></div><div><small>Tarea</small><b>${perf32Esc(perf32Task(base))}</b></div><div><small>Intentos</small><b>${Number(base.attempts||0)}</b></div><div><small>1ª respuesta</small><b>${perf32Minutes(base.first_response_minutes)}</b></div><div><small>Última gestión</small><b>${perf32DateTime(base.last_managed_at)}</b></div><div><small>Próxima acción</small><b>${perf32Esc(perf32Next(base))}</b></div><div><small>Sin gestión</small><b>${perf32Minutes(base.minutes_without_management)}</b></div><div><small>Manual</small><b>${base.manual?'Sí':'No'}</b></div></div><div class="perf32-timeline">${events.map(e=>`<div class="perf32-event"><small>${perf32DateTime(e.at||e.occurred_at)} · ${perf32Esc(e.actor||e.advisor||'Sistema')}</small><b>${perf32Esc(e.title||`Intento #${e.attempt||''}`||'Gestión')}</b>${(e.detail||e.outcome||e.notes)?`<p>${perf32Esc(e.detail||[e.outcome,e.notes].filter(Boolean).join(' · '))}</p>`:''}${e.next_followup_at?`<div class="muted">Próxima acción: ${perf32DateTime(e.next_followup_at)}</div>`:''}</div>`).join('')||'<div class="muted">Todavía no hay gestiones registradas.</div>'}</div>`}catch(e){w.querySelector('.perf32-modal').insertAdjacentHTML('beforeend',`<div class="blocking">${perf32Esc(e?.message||e)}</div>`)}};

/* Integra una sola opción de Rendimiento y elimina las vistas repetidas. */
const perf32RenderShellBase=window.renderShell;
window.renderShell=function(){perf32RenderShellBase();const nav=document.querySelector('.nav'),content=document.querySelector('.content');if(nav){['day-by-day','reports','team','performance'].forEach(v=>nav.querySelector(`[data-view="${v}"]`)?.remove());const b=document.createElement('button');b.dataset.view='performance';b.textContent='Rendimiento';b.onclick=()=>switchView('performance');const sales=nav.querySelector('[data-view="sales"]');nav.insertBefore(b,sales||nav.firstChild)}if(content&&!el('view-performance')){const s=document.createElement('section');s.id='view-performance';s.className='view';const sales=el('view-sales');content.insertBefore(s,sales||content.firstChild)}const foot=document.querySelector('.sidebar-foot');if(foot)foot.innerHTML=foot.innerHTML.replace(/Versión\s+[^<]+/,'Versión 32.0')};
const perf32SwitchBase=window.switchView;
window.switchView=function(view,initial=false){if(['day-by-day','reports','team'].includes(view))view='performance';if(view!=='performance')return perf32SwitchBase(view,initial);currentView='performance';document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));document.querySelectorAll('.nav button').forEach(b=>b.classList.toggle('active',b.dataset.view==='performance'));el('view-performance')?.classList.add('active');if(el('topTitle'))el('topTitle').textContent='Rendimiento';if(!perf32Date)perf32Date=perf32DefaultDate();loadPerformanceV32(false);if(!initial&&innerWidth<800)el('sidebar')?.classList.remove('open')};

console.info('INVICTO OPS v32 · Rendimiento estable activo');