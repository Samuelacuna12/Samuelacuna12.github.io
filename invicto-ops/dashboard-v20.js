/* INVICTO OPS v20 · meta diaria ajustada por hora real de operación */
function bogotaClockV20(){const p=new Intl.DateTimeFormat('en-CA',{timeZone:'America/Bogota',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23',weekday:'short'}).formatToParts(new Date());return Object.fromEntries(p.map(x=>[x.type,x.value]))}
function isWorkingDateV20(y,m,d){return new Date(Date.UTC(y,m-1,d)).getUTCDay()!==0}
function remainingWorkingDaysV20(ms,day,hour){const [y,m]=ms.split('-').map(Number),last=daysInMonthV18(ms);let n=0;for(let d=day;d<=last;d++){if(!isWorkingDateV20(y,m,d))continue;if(d===day&&hour>=18)continue;n++}return Math.max(1,n)}
window.goalStatsV18=function(){const ms=monthStartV18(),arr=salesMonthV18(),confirmed=arr.filter(s=>s.status==='Confirmada').length,target=Number(currentTargetV18()?.target_confirmed_sales||0),p=bogotaClockV20(),day=Number(p.day),hour=Number(p.hour),totalDays=workingDaysV18(ms),elapsedClosed=workingDaysV18(ms,Math.max(0,day-1))+(isWorkingDateV20(Number(p.year),Number(p.month),day)&&hour>=18?1:0),remainingDays=remainingWorkingDaysV20(ms,day,hour),remaining=Math.max(0,target-confirmed),required=target?Math.ceil(remaining/remainingDays):0;let equivalent=elapsedClosed;if(isWorkingDateV20(Number(p.year),Number(p.month),day)&&hour>=8&&hour<18)equivalent+=Math.max(0,Math.min(1,((hour-8)+(Number(p.minute)||0)/60)/10));const projection=equivalent?Math.round(confirmed/equivalent*totalDays):0,pct=target?Math.min(999,Math.round(confirmed/target*100)):0;return {ms,arr,confirmed,target,elapsed:elapsedClosed,totalDays,remainingDays,remaining,required,projection,pct}}
