/* INVICTO OPS v35 legacy compatibility shim.
   Intentionally does nothing. Kept only so cached HTML that still references
   this file cannot enter the old MutationObserver loop. */
console.info('INVICTO OPS · legacy UI shim safe');
